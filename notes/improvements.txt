
Phase 1:

1. Remover Worker-NATS service. The worker delivery should directly receive the batch of data from control plane via natsjetstream. What publishes the data 
   to nats jetstream is another thing to look otu. (done, works good)

2. Make the Worker-Destination independent of database 

3. Add a docker-compose.yml for running Postgres + NATS locally without Kubernetes.








Phase 2:

3. Add state logging endpoints for events (GET event/id results in actual event details from event table, GET /delivery/event/id results in delivery details about an event, GET /events results in all events of the user (limit of 10), one for DLQ too after made)

5. Security: HMAC signing on delivery of webhooks (receiver side )

4. Idempotency Metadata (Very Important) (send which retry this is to user on delivery). 

6. A basic user interface to view event details of a client user.


Phase 3:


7. Testing: Make the system testable, design tests and run them. (Fix the Repository Interface Pattern first)

8. Implement a CI/CD pipeline with gihub actions to deploy with automatic testing.




 
Phase 4:

 1. Rate limiting (onlt POST /events endpoint)
 2. Implement deadletter queue table 
 3. Multi subscriber support (single sub and multi sub mode)
 6. Observability and Monitoring : Implement OpenTelemetry for statistics, add metrics to user interface